#include<stdio.h>

int main(){
    int length,breadth;
    printf("What is the length of the ractangle\n");
    scanf("%d",&length);
    printf("What is the breadth of the ractangle\n");
    scanf("%d",&breadth);
    printf("The area of ractangle is %d \n",length*breadth);
    return 0;
}