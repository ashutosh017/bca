#include<stdio.h>

int main(){
    int radius = 3;
    float pi = 3.14;
    printf("The area of circle is %f \n", pi*radius*radius);
    int height =3;
    printf("The Volume of the cylinder is %f \n", pi*radius*radius*height);
    return 0;
}